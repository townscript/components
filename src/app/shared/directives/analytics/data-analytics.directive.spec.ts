import { DataAnalyticsDirective } from './data-analytics.directive';

describe('DataAnalyticsDirective', () => {
  it('should create an instance', () => {
    const directive = new DataAnalyticsDirective();
    expect(directive).toBeTruthy();
  });
});
